mtcars

git config --global user.email "varun.p18161@iimtrichy.ac.in"
git config --global user.name "pvarunc"

install.packages('car')
plot(women)
?women
?mean
x=1:100
x
y = seq(1,100, by=2)
y
y

install.packages('dplyr')

library(dplyr)

library(modeest)

mlv(x,method='shorth')

quantile(x)
quantile(x,seq(.1,1,by=.1))